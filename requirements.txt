# Local development dependencies.
# torch is the CPU build: it is here to run the package's tests on synthetic tensors,
# not to train. Training happens on Kaggle or a remote machine, where a GPU build is
# already present. Install with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
numpy
pandas
pydicom
opencv-python-headless
matplotlib
scikit-learn
tqdm
ipykernel
ftfy
kaggle
scipy
torch
