{
  "config": {
    "stem": "window",
    "weights": "uniform",
    "epochs": 30,
    "batch_studies": 8
  },
  "run": {
    "split": "train_series",
    "labels": "data/external/riadmohamed42-jev-knee-labels/HYBRID_labels_scores.csv",
    "encoder": "models/dinov2-small",
    "fold": 0,
    "holdout_gold": true
  }
}
